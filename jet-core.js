/* محتوى ملف main.js الذي سترفعه على GitHub */
!function(r,i,t){
    // ... (هنا تضع كل كود الـ function(r,i,t) بالكامل)
}(this,document,setTimeout);

function related_temp(t){ ... }
function sitemap_temp(t){ ... }
function sitemap_list_temp(t){ ... }
function sitemap_cb(t){ ... }
function related_inline_temp(t){ ... }
function sidebar_temp(t){ ... }
function toc_temp(o){ ... }

function jtCallback(){
    // ضع هنا أي استدعاءات إضافية
}
